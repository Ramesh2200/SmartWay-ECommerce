# 26. Postman Testing Guide

Import the included Postman collection located at `docs/postman/smartway_api_collection.json` into Postman. Set environment variable `baseUrl = http://localhost:8080/api` and execute the test runner for automated verification.
