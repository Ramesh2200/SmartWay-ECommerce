# 13. Product Catalog Flow

- 101 distinct products seeded into MySQL.
- 9 master categories with live product count badges.
- Fallback image pipeline rendering custom vector SVGs when external networks are unreachable.
